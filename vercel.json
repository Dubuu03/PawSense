{
  "buildCommand": "curl -L https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.22.0-stable.tar.xz -o flutter.tar.xz && tar xf flutter.tar.xz && git config --global --add safe.directory $(pwd)/flutter && export PATH=\"$PATH:$(pwd)/flutter/bin\" && flutter pub get && flutter build web",
  "outputDirectory": "build/web"
}